#ifndef TOURNAMENT_H
#define TOURNAMENT_H
#include <vector>
#include <string>

namespace test {
    void tournament(const std::vector<std::string>& players_configs, size_t nb_random_moves, size_t nb_games);
}

#endif
